import * as userClient from './user'
import * as campaignClient from './campaign'
import * as pageClient from './page'

export default {
    user: userClient,
    campaign: campaignClient,
    page: pageClient,
}